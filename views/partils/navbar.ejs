<nav class="navbar navbar-expand-xl navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/admin/racks/index">
            <img src="https://i.imgur.com/cTILCjn.png" alt="" height="35">
          </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarBasic" aria-controls="navbarBasic"
            aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarBasic">
            <div class="d-flex ms-auto">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page"
                            href="/admin/racks/new">Cadastrar rack</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page"
                            href="/admin/racks/index">Visualizar racks</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page"
                            href="/admin/racks/select-racks-inversion">Inverter
                            racks</a>
                    </li>
                    <% if (hierarquia == 'adm') { %>
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page"
                                href="/admin/users/new">Cadastrar usuário</a>
                        </li>
                    <% } %> 
                    <% if (hierarquia == 'adm') { %>
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page"
                                href="/admin/users/index">Visualizar usuários</a>
                        </li>
                    <% } %> 
                    <% if (hierarquia == 'adm') { %>
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page"
                                href="/admin/prices/new">Cadastrar preço</a>
                        </li>
                    <% } %> 
                    <% if (hierarquia == 'adm') { %>
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page"
                                href="/admin/prices/index">Visualizar preços</a>
                        </li>
                    <% } %> 
                    <!-- <% if (hierarquia == 'adm') { %>
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page"
                                href="/admin/movements/enter">Cadastrar entrada</a>
                        </li>
                    <% } %>  -->
                    <li class="nav-item">
                        <form action="/admin/users/select-theme" method="post">
                            <input type="hidden" id="urlInput" name="link">
                            <input type="hidden" name="" id="modoAtual" value="<%= theme %>">
                            <% if (theme == 'light') { %>
                                <button type="submit" class="nav-link active bi bi-moon-stars-fill"> Modo escuro</button>
                            <% } else { %>
                                <button type="submit" class="nav-link active bi bi-brightness-high-fill"> Modo claro</button>
                            <% } %>

                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>